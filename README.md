# Финансовый дом — Telegram-бот учёта финансов (MVP)

Бот для учёта доходов и расходов: текст, голос, фото чека. Стек: aiogram 3,
Supabase (Postgres), Groq (LLM + Whisper + Vision), Redis (сессии PIN и FSM),
Render + UptimeRobot.

## ⚠️ Главное правило ввода

**Всегда указывай валюту рядом с суммой.** Без этого бот не может отличить
сумму от количества или массы.

- ✅ `кофе 150р`, `зарплата 100000₽`, `такси 350р`, `lunch $12`
- ❌ `сахар 5` — непонятно: 5 килограмм или 5 рублей?

Поддерживаемые обозначения валюты: `р`, `руб`, `₽`, `$`, `usd`, `€`, `eur`.

## Установка

⚠️ В репозитории есть файл `.python-version` (закрепляет Python 3.12) —
не удаляй его. Без него Render на новых сервисах подхватывает самую свежую
версию Python (сейчас 3.14), а для нее у некоторых зависимостей
(`pydantic-core`, тянется через `aiogram`) ещё нет готовых собранных
пакетов — pip пытается собрать их из исходников через Rust/Cargo, а
песочница сборки на Render это запрещает, деплой падает на этапе
установки зависимостей. Если уже есть развёрнутый сервис и появился этот
файл впервые — на Render нужно **Clear build cache & deploy**, обычного
редеплоя может быть недостаточно (кеш от старой версии Python).

⚠️ В `requirements.txt` версии зафиксированы как **минимальные** (`>=`), не
точные (`==`) — специально. Дважды за время разработки точная фиксация
приводила к рабочему падению не из-за нашего кода, а из-за того, что
зафиксированная версия SDK успевала устареть относительно внешнего
сервиса (Supabase сменил формат ключей — старый `supabase-py` не умел его
валидировать; Groq добавил параметр `reasoning_effort` — старый `groq` SDK
о нём не знал). `>=` даёт pip брать самую свежую совместимую версию при
каждой установке, что чаще спасает от такого дрейфа, чем ловит. Обратная
сторона: где-то в будущем свежая версия библиотеки теоретически может
принести breaking change — но на практике это встречается ощутимо реже,
чем "старая версия не умеет то, что сейчас нужно". **Не перезаписывай эти
строки обратно на `==`**, если не уверен, что именно должно быть жёстко
закреплено.

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env  # заполнить токены
```

Выполнить `schema.sql` в Supabase SQL editor.

### Переменные окружения (дополнительно к базовым)

- `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` — тот же OAuth-клиент, что у
  PixKeep (Google Cloud проект общий, приложения разные)
- `GOOGLE_OAUTH_REDIRECT_URI` — свой домен, напр.
  `https://<этот-render-сервис>.onrender.com/oauth/callback` (ОТДЕЛЬНЫЙ от
  PixKeep, т.к. другой Render-сервис)
- `CRON_SECRET` — произвольная строка; защищает `/cron/daily` от
  посторонних вызовов (см. ниже)
- `GUIDE_URL` — ссылка на инструкцию (Telegraph), опционально — уже есть
  дефолт в коде; переопредели, если переопубликуешь гайд по новому URL

### Ежедневный cron (напоминания о пробеге + месячная статистика + годовой отчёт)

Настрой внешний пинг (UptimeRobot, как и для `/health`) раз в день на:
```
GET https://<этот-домен>.onrender.com/cron/daily?secret=<CRON_SECRET>
```
Один пинг закрывает три задачи — каждая сама решает, нужно ли что-то делать
именно сегодня: напоминание про пробег — раз в неделю на машину (трекается
независимо для каждой), статистика — раз в отчётный период на пользователя
(по его собственному дню начала периода, не календарный конец месяца),
годовой отчёт — 1 января (любое время в течение дня, когда сработает пинг;
защита от повторной отправки через Redis-флаг на весь год).

### Устойчивость (как в остальных твоих ботах)

- **Автоперезапуск polling** — если `dp.start_polling()` упадёт по сетевому
  сбою и т.п., бот сам перезапустит его через 5 сек, а не умрёт целиком
  (`run_polling_with_retry` в `main.py`).
- **Supabase keep-alive** — фоновая задача внутри бота стучится в Supabase
  каждые `SUPABASE_KEEPALIVE_INTERVAL_SECONDS` (по умолчанию 6 часов),
  независимо от внешних cron-пингов — free-tier проекты Supabase
  приостанавливаются после ~7 дней без обращений к API.

Запуск:

```bash
python main.py
```

## Структура проекта (плоская, без подпапок)

**Инфраструктура / хранение**

| Файл | Назначение |
|---|---|
| `config.py` | Переменные окружения, константы (категории, ключевые слова) |
| `supabase_client.py` | Метаданные: пользователи, категории, семья, PIN, Google-токены |
| `sheets_client.py` | Низкоуровневый Sheets/Drive API (создание, чтение, запись, ретраи 500/503/429) |
| `sheets_transactions.py` | Транзакции поверх Sheets: сохранение, чтение по диапазону дат |
| `google_oauth.py`, `google_oauth_web.py` | OAuth-флоу подключения Google Drive |
| `google_api.py` | Обновление access-токена на 401 + повтор именно упавшего запроса |
| `period_utils.py` | Общая логика дат/периодов (без импортов из остального проекта — чтобы не давать циклических зависимостей) |
| `middlewares.py` | Гейт по PIN-коду |

**Ввод трат и категоризация**

| Файл | Назначение |
|---|---|
| `parser.py` | Парсинг суммы/валюты/типа/количества («по») из текста |
| `groq_client.py` | LLM-категоризация, Whisper, Vision (чеки), разбор чат-вопросов |
| `input_handler.py` | Текст/голос/фото — основной ввод трат, роутинг Авто-категории и вопросов |
| `insights.py` | Чат-вопросы по накопленным данным (суммы, «когда», сравнения, топ-категория, счётчик/среднее) |
| `narrative_report.py` | Связный отчёт со сравнением периодов; годовой отчёт 1 января |

**Машины**

| Файл | Назначение |
|---|---|
| `cars.py` | Регистрация машин, пробег, авто-траты — поверх Sheets |
| `cars_command.py` | `/cars` — список, добавить, удалить (с экспортом истории) |
| `car_stats.py` | Статистика по машине (данные + автоматическая ежемесячная рассылка) |
| `car_stats_command.py` | `/carstats` |
| `auto_expense.py` | Эвристика типа авто-траты (Заправка/ТО/Ремонт/Прочее) |
| `fluid_tracker.py` | Отслеживание интервалов замены жидкостей |
| `reminders.py` | Еженедельные напоминания о пробеге |

**Команды и онбординг**

| Файл | Назначение |
|---|---|
| `states.py` | FSM-состояния |
| `keyboards.py` | Inline-клавиатуры |
| `start.py` | `/start` + онбординг |
| `pin_auth.py`, `pin_handler.py` | Проверка PIN при входе, хеш + сессии в Redis |
| `report.py` | `/report` (+ сравнение с предыдущим периодом) |
| `history.py` | `/history [дни]` |
| `categories.py` | `/categories` — просмотр/переименование/добавление |
| `family.py` | `/family @username`, `/family leave` |
| `settings.py` | `/settings` — валюта, период, PIN |
| `undo.py` | `/undo` — отмена последней записи |
| `main.py` | Точка входа, регистрация роутеров, cron-эндпоинты |

## Что входит в MVP

- Три способа ввода: текст, голос (Whisper), фото чека (Vision — только сумма)
- Категоризация: ключевые слова → память (`category_map`) → Groq LLM
- Ambiguous-флоу: если категория не ясна — inline-кнопки, выбор запоминается
- `/report`, `/history`, `/categories`, `/family`, `/undo`, `/settings`
- Онбординг: валюта → день периода → сумма на руках (опц.) → Google Drive
  (обязательно) → машина(ы) (опц., цикл добавить/готово) → PIN (опц.)
- Семейный бюджет: общий кошелёк (одна Google-таблица на семью, владелец —
  инициатор `/family`), без расчёта "кто кому должен"
- PIN-код (Supabase-хеш + Redis-сессия 5 мин, блокировка после 3 попыток)
- Реакция 👍 вместо текстового подтверждения записи
- **Хранение**: реальные транзакции — в Google Sheets владельца (не в
  Supabase!). Supabase — только метаданные (users/категории/семья/PIN).
- **Авто-траты**: категория «Авто» → отдельный лист с типом (Заправка/ТО/
  Ремонт/Прочее), машиной, пробегом; поддержка нескольких машин на семью с
  дизамбигуацией через кнопки
- **Пробег без валюты**: «пробег опель 305000 км» — отдельная ветка, не
  требует указания валюты
- **Еженедельные напоминания о пробеге** — по каждой машине отдельно (внешний
  cron-пинг на `/cron/daily`, см. `reminders.py`), кнопка «Без изменений»
- **Отслеживание техжидкостей** — предупреждение при приближении к интервалу
  замены (масло/антифриз/тормозная жидкость/фильтр/свечи), без привязки к
  конкретному двигателю; срабатывает при каждой новой записи пробега
- **`/cars`** — список машин с удалением (мягкий архив + выгрузка полной
  истории в отдельный файл), добавление новой
- **`/carstats`** — статистика по машине за текущий период (пробег +
  разбивка трат по типам); плюс автоматическая рассылка в последний день
  периода (тот же `/cron/daily`) — всем, кто делит эту таблицу
- **Количество товара**: «2 мороженых по 4 рубля» = 8 (не 4) — умножение
  только с явным маркером «по», без него число просто игнорируется как
  безопасный фоллбэк (не гадаем, что имелось в виду)
- **«➕ Добавить категорию»** — кнопка прямо в клавиатуре неоднозначной
  категории, не нужно отдельно идти в `/categories`
- **Чат-вопросы по накопленным данным** (`insights.py`) — LLM только
  разбирает вопрос в структуру (категория/товар/машина/период/тип ответа),
  считает наш код, без выдумывания цифр моделью:
  - сумма трат/доходов, в т.ч. по конкретному товару вне списка категорий
  - «когда» — дата(ы) покупки, включая товар+машина одновременно
  - сравнение периодов («в этом месяце больше, чем в прошлом?»)
  - топ-категория трат за период
  - счётчик и среднее («сколько раз», «сколько в среднем»)
  - разговорная память 10 минут — уточняющие вопросы («а в июле?»)
  без повторения контекста
- **Связный отчёт** — `/report` дополняется сравнением с предыдущим
  периодом (только заметные изменения, порог 20%+, без общих советов)
- **Годовой отчёт** — 1 января, сравнение прошедшего года с годом до него,
  тем же принципом, что и `/report`

## Не входит в MVP (бэклог)

Экспорт Excel/PDF отдельно от Sheets, шаблоны команд, `/edit`, `/find`,
графики в отчётах, средний чек, регулярные платежи, интеграции с банками,
персонализация интервалов замены жидкостей под конкретный двигатель (сейчас
общие ориентиры по типу жидкости, не по марке/модели), построчный разбор
чека (сейчас только общая сумма), разбивка списка товаров одним сообщением,
количество товара как отдельная величина (участвует только в расчёте
суммы, не хранится отдельно), item+машина в топ-категории/сравнении
периодов (сейчас есть только в обычном поиске по сумме).

## Известные допущения / TODO перед продакшеном

- **Модели Groq актуальны на момент написания** (`openai/gpt-oss-20b` для
  категоризации/разбора вопросов, `qwen/qwen3.6-27b` для фото чеков,
  `whisper-large-v3` для голоса) — Groq время от времени отключает старые
  модели (уже переживали это с `llama-4-scout-17b-16e-instruct`), сверяй
  список актуальных моделей в консоли Groq, если начнут сыпаться ошибки
  категоризации/фото после долгого перерыва в деплое.
- `PIN_SALT` уже вынесен в `.env` — обязательно задай свою случайную строку
  перед первым запуском в проде (не используй дефолт из кода).
- Reactions (`set_message_reaction`) требуют Bot API 7.0+ — проверить версию
  aiogram/Telegram Bot API на момент деплоя.
- Для группы более 2 человек в `/family` текущая схема уже поддерживает N
  участников (family_members — многие-ко-многим), тестировать на 2 сначала.
- Чат-вопросы про машину (`item`+`car_name` вместе) сейчас поддержаны
  только для суммы/«когда»/счётчика/среднего — сравнение периодов и
  топ-категория пока не учитывают конкретную машину, если она упомянута
  (см. бэклог выше).
